diff --git a/Minimig.sv b/Minimig.sv
--- a/Minimig.sv
+++ b/Minimig.sv
@@ -122,5 +122,6 @@ wire de25_pll_busy;
 wire de25_pll_done;
 wire de25_pll_error;
+wire de25_control_reset;
 wire de25_ram_quiesced;
 wire de25_ram_active;
 wire de25_sdram_controller_quiesced;
@@ -144,6 +145,7 @@ de25_minimig_pll pll
 	.busy(de25_pll_busy),
 	.done(de25_pll_done),
-	.error(de25_pll_error)
+	.error(de25_pll_error),
+	.control_reset(de25_control_reset)
 );
 
 de25_sdram_quiesce de25_ram_quiesce
@@ -152,6 +154,6 @@ de25_sdram_quiesce de25_ram_quiesce
 (
 	.clk_control(CLK_50M),
-	.reset_control(RESET),
+	.reset_control(de25_control_reset),
 	.clk_sdram(clk_114),
 	.pll_locked(locked),
 	.request_control(de25_reconfig_request),
@@ -164,7 +166,7 @@ always @(posedge CLK_50M) begin
 	de25_want_ntsc_sync <= {de25_want_ntsc_sync[0], ntsc};
 	de25_reconfig_start <= 0;
 
-	if(RESET) begin
+	if(de25_control_reset) begin
 		de25_pll_profile_ntsc <= 0;
 		de25_pll_applied_ntsc <= 0;
 		de25_reconfig_request <= 0;
