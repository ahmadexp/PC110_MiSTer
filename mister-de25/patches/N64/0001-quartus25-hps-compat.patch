diff --git a/sys/hps_io.sv b/sys/hps_io.sv
--- a/sys/hps_io.sv
+++ b/sys/hps_io.sv
@@ -254,6 +254,15 @@ reg [31:0] ps2_key_raw = 0;
 wire       pressed  = (ps2_key_raw[15:8] != 8'hf0);
 wire       extended = (~pressed ? (ps2_key_raw[23:16] == 8'he0) : (ps2_key_raw[15:8] == 8'he0));
 
+reg  [7:0] kbd_data;
+reg        kbd_we;
+wire [8:0] kbd_data_host;
+reg        kbd_rd;
+reg  [7:0] mouse_data;
+reg        mouse_we;
+wire [8:0] mouse_data_host;
+reg        mouse_rd;
+
 reg [MAX_W:0] byte_cnt;
 reg   [3:0] sdn_ack;
 wire [15:0] disk = 16'd1 << io_din[11:8];
@@ -561,11 +570,6 @@ generate
 			end
 		end
 
-		reg  [7:0] kbd_data;
-		reg        kbd_we;
-		wire [8:0] kbd_data_host;
-		reg        kbd_rd;
-
 		ps2_device keyboard
 		(
 			.clk_sys(clk_sys),
@@ -584,11 +588,6 @@ generate
 			.rd(kbd_rd)
 		);
 
-		reg  [7:0] mouse_data;
-		reg        mouse_we;
-		wire [8:0] mouse_data_host;
-		reg        mouse_rd;
-
 		ps2_device mouse
 		(
 			.clk_sys(clk_sys),
@@ -1031,9 +1030,12 @@ module confstr_rom #(parameter CONF_STR, STRLEN)
 	output reg [7:0] conf_byte
 );
 
-reg [7:0] rom[STRLEN];
+localparam ADDR_WIDTH = $clog2(STRLEN+1);
+localparam ROM_DEPTH = 1 << ADDR_WIDTH;
+reg [7:0] rom[ROM_DEPTH];
 
 initial begin
+	for(int i = 0; i < ROM_DEPTH; i++) rom[i] = 0;
 	if( CONF_STR=="" )
 		$readmemh("cfgstr.hex",rom);
 	else
