diff --git a/menu.sv b/menu.sv
index ece9dee..c7bd8e1 100644
--- a/menu.sv
+++ b/menu.sv
@@ -28,6 +28,7 @@ module emu
 wire        locked;
 wire        clk_sys;
 wire        de25_sdram_clk;
+wire        de25_sdram_capture_clk;
 reg         ce_pix;
 wire        forced_scandoubler;
 wire [127:0] status_bus;
@@ -108,6 +109,7 @@ pll pll
 	.outclk_0(clk_sys),
 	.outclk_1(CLK_VIDEO),
 	.outclk_2(de25_sdram_clk),
+	.outclk_3(de25_sdram_capture_clk),
 	.locked(locked)
 );
 
@@ -122,7 +124,7 @@ sdram sdr
 (
 	.init(~locked),
 	.clk(clk_sys),
-	.clk_capture(de25_sdram_clk),
+	.clk_capture(de25_sdram_capture_clk),
 	.SDRAM_DQ_IN(de25_sdram_dq_in),
 	.SDRAM_DQ_OUT(sdram_dq_out),
 	.SDRAM_DQ_OE(sdram_dq_oe),
diff --git a/rtl/sdram.sv b/rtl/sdram.sv
index 5264dcf..72279e6 100644
--- a/rtl/sdram.sv
+++ b/rtl/sdram.sv
@@ -89,8 +89,8 @@ reg        chip = 0;
 reg [15:0] data;
 reg [15:0] data_capture = '0;
 
-// Capture returning data with the same phase-shifted clock that is forwarded
-// to the SDRAM. The controller consumes this register on a later clk edge.
+// Capture returning data with a phase-shifted clock derived alongside the
+// forwarded SDRAM clock. The controller consumes it on a later clk edge.
 always @(posedge clk_capture) data_capture <= SDRAM_DQ_IN;
 
 assign SDRAM_nCS  = chip;
